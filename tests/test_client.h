#pragma once

#include <gtest/gtest.h>
#include "client.h"
#include "manger.h"


TEST(ClientSocketTest, IsValidPointer) {
    
    ClientSocket socket(11);
    // ASSERT_EQ(1 , socket.is_valid_size(10));

}
